import React from 'react';
import Header from '../components/Header';

const SearchDetail = () => {
    return (
        <div>
            <Header />
        </div>
    );
};

export default SearchDetail;
